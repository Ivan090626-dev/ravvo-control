import{db}from"./db.js";
export type GroupSettings={welcomeEnabled:boolean;welcomeText:string;goodbyeEnabled:boolean;goodbyeText:string;captchaEnabled:boolean;captchaMinutes:number;antiLinks:boolean;allowedDomains:string[];antiFlood:boolean;floodMessages:number;floodSeconds:number;antiCaps:boolean;capsPercent:number;badWordsEnabled:boolean;badWords:string[];blockForwards:boolean;blockMedia:boolean;warnLimit:number;warnAction:"mute"|"kick"|"ban";logActions:boolean;deleteServiceMessages:boolean};
export const defaults:GroupSettings={welcomeEnabled:true,welcomeText:"Добро пожаловать, {user}, в {group}! Ознакомьтесь с правилами: /rules",goodbyeEnabled:false,goodbyeText:"{user} покинул(а) сообщество.",captchaEnabled:false,captchaMinutes:3,antiLinks:false,allowedDomains:[],antiFlood:true,floodMessages:6,floodSeconds:8,antiCaps:false,capsPercent:75,badWordsEnabled:false,badWords:[],blockForwards:false,blockMedia:false,warnLimit:3,warnAction:"mute",logActions:true,deleteServiceMessages:false};
export async function getSettings(groupId:string){const g=await db.group.findUnique({where:{id:groupId},select:{settings:true}});try{return{...defaults,...JSON.parse(g?.settings||"{}")}as GroupSettings}catch{return{...defaults}}}
export async function saveSettings(groupId:string,value:Partial<GroupSettings>){const settings={...defaults,...value};await db.group.update({where:{id:groupId},data:{settings:JSON.stringify(settings)}});return settings}
