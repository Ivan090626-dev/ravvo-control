import{db}from"./db.js";
export const permissions=["BAN","MUTE","KICK","DELETE","ROLE_MANAGE","RULES_MANAGE","ANNOUNCE","VIEW_LOG"] as const;export type Permission=typeof permissions[number];
export async function allow(groupId:string,userId:string,p:Permission){const m=await db.member.findUnique({where:{groupId_telegramId:{groupId,telegramId:userId}},include:{roles:{include:{role:true}}}});if(m?.nativeAdmin)return true;return m?.roles.some(x=>{try{return(JSON.parse(x.role.permissions)as string[]).includes(p)}catch{return false}})??false}
export async function defaultRoles(groupId:string){const all=[...permissions],items=[{name:"owner",color:"#F5B84B",p:all},{name:"moderator",color:"#7C5CFF",p:["BAN","MUTE","KICK","DELETE","VIEW_LOG"]},{name:"editor",color:"#36CFA0",p:["RULES_MANAGE","ANNOUNCE","VIEW_LOG"]}];for(const r of items)await db.role.upsert({where:{groupId_name:{groupId,name:r.name}},update:{},create:{groupId,name:r.name,color:r.color,permissions:JSON.stringify(r.p),system:true}})}
